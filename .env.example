# --- Credentials to change. ---

DB_USER = 'YOUR_USER'
DB_PASS = 'YOUR_PASSWORD'
DB_HOST = 'localhost' # Usual host for local databases.
DB_PORT = '5432' # Default PostgreSQL port.
DB_NAME = 'YOUR_DATABASE_NAME'

# ^^^ Credentials to change. ^^^